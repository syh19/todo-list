// src/store/index.ts

import Vue from 'vue'
import Vuex from 'vuex'

// import TodoItemModule from './todoItemModule'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    // myMod: TodoItemModule
  }
})